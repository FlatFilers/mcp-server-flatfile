/**
 * This file was generated. Do NOT edit this file.
 */

import fetch from 'node-fetch'
import { type ZodRawShape, z } from 'zod'
import { actions_APIActionConfigsSchema, commons_SpaceIdSchema } from '../schemas.js'
import type { Tool } from './index.js'

const params = {
  spaceId: commons_SpaceIdSchema.describe('The Space ID for which to create the Actions.'),
  body: actions_APIActionConfigsSchema,
} as ZodRawShape

export const bulkCreateActions: Tool<typeof params> = {
  name: 'bulkCreateActions',
  description: 'Create several actions: Create several actions',
  params,
  cb: async ({ spaceId, body }) => {
    try {
      const searchParams = { spaceId }

      const searchParamsString = new URLSearchParams(JSON.parse(JSON.stringify(searchParams))).toString()
      const baseUrl = process.env.FLATFILE_API_URL || 'https://platform.flatfile.com/api/v1'
      const url = `${baseUrl}/actions/bulk${searchParamsString ? `?${searchParamsString}` : ''}`

      const response = await fetch(url, {
        method: 'POST',
        headers: {
          'X-Disable-Hooks': 'true',
          Authorization: `Bearer ${process.env.FLATFILE_BEARER_TOKEN}`,
          'Content-Type': 'application/json',
        },
        body: JSON.stringify(body),
      })

      if (!response.ok) {
        throw new Error(`HTTP error! Status: ${response.status} - ${response.statusText}`)
      }

      const data = (await response.json()) as { data: unknown }

      return {
        content: [
          {
            type: 'text',
            text: JSON.stringify(data.data, null, 2),
          },
        ],
        status: 'success',
      }
    } catch (error) {
      return {
        content: [{ type: 'text', text: `Error: ${error}` }],
        status: 'failed',
      }
    }
  },
}
